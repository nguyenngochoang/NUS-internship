<h1>Hello boss!</h1>
<%= link_to 'Add new employee', new_employee_path %>
<table>

  <tr>
    <th>ID</th>
    <th>name</th>
    <th>phone</th>
  </tr>
  <% @employee.each do |item| %>
  <tr>
      <td> <%= item.id%> </td>
      <td><%= item.name%></td>
      <td> <%= item.phone%> </td>
      <td> <%= link_to 'Show', employee_path(item) %> </td>
      <td> <%= link_to 'Edit', edit_employee_path(item) %> </td>
      <td><%= link_to 'Delete', employee_path(item),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>

      </td>
      <td> <%= link_to 'Add a customer', new_employee_customer_path(item) %> </td>
      <td> <%= link_to 'Show all customers', employee_customers_path(item) %> </td>
    <% end %>
  </tr>

</table>

<%= link_to 'Back', root_path %>