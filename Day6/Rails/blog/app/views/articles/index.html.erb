<%= link_to 'New article', new_article_path %>
<table>
    <tr>
        <th>title</th>
        <th>text</th>
    
    </tr>
    <% @articles.each do |item| %>
        <tr>
            <td> <%= item.title%>  </td>
            <td> <%= item.text%> </td>
            <td><%= link_to 'Show', article_path(item) %></td>
            <td><%= link_to 'Edit', article_path(item) %></td>
            <td><%= link_to 'Destroy', article_path(item),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>
    </tr>
            
        </tr>
    <% end %>
</table>
<%= link_to 'Landing page', controller: 'welcom' %>